# レジスタファイルのリードは非同期回路、ライトは同期でないと書き込めない
これはライトの場合は書き込み先のレジスタが異なるため
入力ファイルを全てにファンアウトし、使用するかどうかといった非同期回路も作成可能だが。。。

# icarus verilog では enum に同一の値は許されない

# メモリの初期化方法を調べる
$readmemh()
BRAM を推論させる必要な無く、モジュール内の reg 配列を指定して初期化

# Makefile 内での繰り返し分